//user/

import express from "express"
import {dirname} from 'path'
import { fileURLToPath } from "url"

const app = express()

dirname(fileURLToPath(import.meta.url))

app.set('views')

const _dirname = dirmane(fileURLToPath(import.meta.url))
console.log(_dirmane)




